{
  "name": "checkout-bot-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Defensive red-team demo of scalper/checkout bot techniques. Run against your own/test store only.",
  "type": "module",
  "scripts": {
    "watch": "tsx src/index.ts --mode=monitor",
    "run": "tsx src/index.ts --mode=checkout",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "playwright": "^1.49.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.0",
    "@types/node": "^20.0.0"
  }
}
